# .github
Apache Pinot real-time analytics turns streaming events into fast, queryable insight. As an OLAP database, Apache Pinot query latency stays low even at high concurrency, so dashboards, metrics, and user-facing analytics respond in milliseconds. Apache Pinot keeps ingestion, storage, and query in one calm system built for scale.
